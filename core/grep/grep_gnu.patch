--- a/src/dfasearch.c	2010-09-21 14:37:23.000000000 +0000
+++ b/src/dfasearch.c	2010-09-21 14:37:49.000000000 +0000
@@ -61,10 +61,10 @@
 void
 dfawarn (char const *mesg)
 {
-  static enum { NONE = 0, POSIX, GNU } mode;
+  static enum { NONE = 0, POSIX, MODE_GNU } mode;
   if (mode == NONE)
-    mode = (getenv ("POSIXLY_CORRECT") ? POSIX : GNU);
-  if (mode == GNU)
+    mode = (getenv ("POSIXLY_CORRECT") ? POSIX : MODE_GNU);
+  if (mode == MODE_GNU)
     dfaerror (mesg);
 }
 
