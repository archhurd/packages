service login
{
        flags                   = REUSE
        socket_type             = stream
        wait                    = no
        user                    = root
        server                  = /sbin/rlogind
        log_on_failure          += USERID
        disable                 = yes
}
