service shell
{
        flags                   = REUSE
        socket_type             = stream
        wait                    = no
        user                    = root
        server                  = /sbin/rshd
        log_on_failure          += USERID
        disable                 = yes
}
